# Floating Orbit Tabs

## What does this do?

CSS-only tabs where the active tab floats above others with a translateY and scale transition.

## How is it used?

Add `.floating-orbit-tabs` to a container with radio inputs, labels, and `.tab-panel` divs.

## Why is it useful?

Lightweight, keyboard-accessible tabs with no JavaScript. The floating effect highlights the active step.
